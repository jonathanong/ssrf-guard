--- src/core/url-safety.mts
+++ src/core/url-safety.mts
@@ -21,8 +21,8 @@
   try {
     const parsed = new URL(url);
     if (parsed.username || parsed.password) {
-      parsed.username = "***";
-      parsed.password = "***";
+      parsed.username = "***"; // NOSONAR
+      parsed.password = "***"; // NOSONAR
       return parsed.href;
     }
   } catch {
